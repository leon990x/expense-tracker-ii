# Expense Dashboard

`ExpenseDashboard` is the main expense view. It splits expenses into daily, weekly, and monthly groups, computes totals and budget status, and renders three `SummaryTile` sections.

It also manages inline expense creation, editing, deletion, and bulk category updates using optimistic UI patterns before syncing via server actions.
