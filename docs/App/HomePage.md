# Home Page

The home route (`src/app/page.tsx`) fetches expenses and budget data on the server, then renders `ExpenseDashboard` inside the primary dashboard container.

The page is marked dynamic (`force-dynamic`) so current data is always fetched.
