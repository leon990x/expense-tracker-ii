# Expense

`src/types/expense.ts` defines the core expense domain model: `Category`, `Expense`, and `DailySummary`.

`Category` is a strict string union used across UI and data layers to keep category handling type-safe.
