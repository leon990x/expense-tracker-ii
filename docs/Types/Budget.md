# Budget

`src/types/budget.ts` defines budget-focused types: `BudgetTimeframe`, `BudgetLimits`, `BudgetData`, and `BudgetCategoryRow`.

These types structure both persisted budget limits and computed category spending rows shown in budget views.
