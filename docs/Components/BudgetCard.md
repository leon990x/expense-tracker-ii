# Budget Card

`BudgetCard` displays one timeframe’s budget rows by category, including spent amount, limit value, and a progress bar state (normal, near limit, over budget).

It supports inline limit editing, optimistic local updates, and persistence via `updateBudgetLimit`, including a `No Limit` mode represented by `-1`.
