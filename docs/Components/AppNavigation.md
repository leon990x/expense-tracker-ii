# App Navigation

`AppNavigation` renders top-level links for Dashboard (`/`) and Budget (`/budget`). It uses `usePathname` to style the active route and keeps navigation logic minimal and client-side.
